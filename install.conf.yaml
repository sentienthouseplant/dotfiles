- plugins:
    - dotbot-brew/

# Installs brew if missing
- install-brew: true

- brew:
    - zsh
    - eza
    - zoxide
    - bat
    - uv

- cask:
  - kitty

- clean: ['~']

- link:
    ~/.profile: profile
    ~/.gitconfig: gitconfig
    ~/.zprofile: zprofile
    ~/.zshenv: zshenv
    ~/.zshrc: zshrc
    ~/.config/nvim: nvim

- create:
    ~/repos:
